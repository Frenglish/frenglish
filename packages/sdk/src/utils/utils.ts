import { PartialConfiguration } from '@frenglish/utils';

/**
 * Parses and returns a partial configuration.
 * This is a placeholder for any future validation or transformation logic.
 *
 * @param partialConfig - A partial project configuration object.
 * @returns The same partial configuration, unmodified.
 */
export async function parsePartialConfig(partialConfig: PartialConfiguration): Promise<PartialConfiguration> {
  return partialConfig;
}
